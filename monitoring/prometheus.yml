global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Note: tinyproxy-ng doesn't expose Prometheus metrics yet
  # This is a placeholder for future implementation
  # - job_name: 'tinyproxy-ng'
  #   static_configs:
  #     - targets: ['proxy-server:26128']
